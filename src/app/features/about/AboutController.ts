/// <reference path="../../../_references.d.ts" />

'use strict';
import * as angular from 'angular';

class AboutController {
    constructor () {
    }
}

export default angular.module('aboutController', [])
    .controller('aboutController', AboutController);
